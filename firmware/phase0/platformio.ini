[env:esp32dev]
platform  = espressif32
board     = esp32dev
framework = arduino

; Serial Monitor speed — must match the value in main.cpp
monitor_speed = 115200

; Upload speed
upload_speed = 921600
